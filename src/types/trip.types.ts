export type Trip = {
  departure: {
    location: string
    address: string
    buildingCode: string
  }
  arrival: {
    location: string
    address: string
    buildingCode: string
  }
  env: string
  ac: string
  deb: string
  pec: string
  dep: string
  fin: string
}
